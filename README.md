# AI-chatbot

Created Just as a learning experince too see if I could make my own ai chat bot using the gemini api.
Too use make sure you've done:
``pip install google-genai``

From there it should run - just make sure to add your api key into the api key area at the top
